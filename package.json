{
  "name": "tis-tales",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "project-setup": "echo 'Project setup complete'",
    "build": "node scripts/themeGenerator.js && hugo --gc --minify --templateMetrics --templateMetricsHints --forceSyncStatic",
    "dev": "node scripts/themeGenerator.js && hugo server --buildDrafts --bind 0.0.0.0 --port 1313"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.0",
    "@tailwindcss/forms": "^0.5.11",
    "@tailwindcss/typography": "^0.5.19",
    "tailwind-bootstrap-grid": "^7.0.0",
    "tailwindcss": "^4.3.0"
  }
}